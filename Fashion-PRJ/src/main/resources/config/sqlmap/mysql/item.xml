<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ejo.repository.mapper.ItemMapper">
    <!-- 아이템 조회 -->
    <select id="selectItemFile" resultType="itemFile">
    	select code_value as codeValue, file_path as filePath, ori_file_name as oriFileName, real_file_name as realFileName
 		  from t2_item_file
    </select>
</mapper>